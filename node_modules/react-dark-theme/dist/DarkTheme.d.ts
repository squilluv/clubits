/**
 * @class DarkTheme
 */
import React from 'react';
import PropTypes from 'prop-types';
import 'react-toggle/style.css';
import { Theme } from './index';
export declare type Props = {
    light: Theme;
    dark: Theme;
    defaultDark: boolean;
    className?: string;
};
declare type State = {
    dark: boolean;
};
declare class DarkTheme extends React.Component<Props, State> {
    static propTypes: {
        light: PropTypes.Validator<object>;
        dark: PropTypes.Validator<object>;
        defaultDark: PropTypes.Requireable<boolean>;
        className: PropTypes.Requireable<string>;
    };
    static defaultProps: {
        defaultDark: boolean;
        className: undefined;
    };
    constructor(props: Props);
    handleToggleChange: () => void;
    render(): JSX.Element;
}
export default DarkTheme;
