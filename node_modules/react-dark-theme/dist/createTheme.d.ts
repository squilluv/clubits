import { Theme } from './index';
export declare const verifyTheme: (dark: Theme, light: Theme) => void;
/**
 * Used to create a map CSS variables in your CSS-in-JS solution. Use these properties as values in your CSS.
 *
 * @param dark
 * @param light
 */
declare function createTheme<T extends Theme>(dark: T, light: T): T;
export default createTheme;
